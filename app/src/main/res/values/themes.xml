<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Theme -->
    <style name="Base.Theme.AI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- ✅ Set both versions -->
        <item name="android:statusBarColor">@color/botMessageBg</item>

        <!-- Optional: Navigation bar color -->
        <item name="android:navigationBarColor">@color/botMessageBg</item>
    </style>

    <style name="Theme.AI" parent="Base.Theme.AI" />
</resources>
